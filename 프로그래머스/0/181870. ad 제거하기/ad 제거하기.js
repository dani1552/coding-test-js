function solution(strArr) {
    let result = [];
    for (let s of strArr) {
        if (s.includes("ad")) {
            result.push();
        } else {
            result.push(s);
        }
    }
    
    return result;
}